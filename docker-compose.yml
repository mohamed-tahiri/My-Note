version: "3.9"
services:
  # backend:
  #   build: ./backend
  #   ports:
  #     - "3001:3001"
  #   depends_on:
  #     - db
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: appdb
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
